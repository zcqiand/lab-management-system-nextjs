node_modules/
.next/
out/
coverage/
.state/trace.json
.state/gate.json
.env*.local

# infra-only: emit 产物由 emit-schema.mjs 重出，不入 git
/generated/

# orval codegen：npm run gen:shared 重出，不入 git
/src/api/endpoints/

# TypeScript build cache（incidental；L3 typecheck 自动跑后会产）
tsconfig.tsbuildinfo

# PG 本地连接池临时文件（如有）
data/*.db
data/*.db-journal

# Drizzle 生成产物：迁移 SQL 由 db:generate 重出
/drizzle/

# 杂
.DS_Store
*.log

# Claude 临时文件
.claude/
.superpowers/

# .env.production: 部署契约真值(含 secret),gitignored 不进 git(2026-08-28 起,同 .env.local 模式)
# key 集合由 suite L0.5 门核对(存在时与 .env.example 严格双向比对);真值经 scripts/sync-secrets 进 GitHub Secrets
.env.production
